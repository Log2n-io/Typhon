{
    "_comment": "Test-only telemetry config. Enables the Tracy-style profiler for ProfilerActive==true so TyphonEvent producer tests can exercise the on-path. Concurrency stays off — ConcurrencyTracingStressTests is [Explicit] and requires running with TYPHON__PROFILER__CONCURRENCY__ENABLED=true env var. Checks (strict mode, #422) run suite-wide so the converted user-facing guards actively catch misuse; DeclaredAccess is on so the SystemAccessValidator net is exercised.",
    "_comment_trace": "Trace=none (#616): collect, but write no capture file. Since #616 an ABSENT trace path means 'a capture in the database's profilings/ directory', which for this suite would mean a file per runtime-creating test — ~20s of pure I/O and a FileExporter attached to the process-global profiler that other fixtures do not expect. The producer path is still fully exercised; only export is off.",
    "Typhon": {
        "Profiler": {
            "Enabled": true,
            "Trace": "none",
            "CpuSampling": { "Enabled": true }
        },
        "Checks": {
            "Enabled": true,
            "DeclaredAccess": true
        }
    }
}
